---
description: Generate a content/blog plan for a given duration
argument-hint: "[duration, e.g. '90 days' or '6 months']"
---

Based on `client-brief/business-goals.md` (target customer, brand position) and `client-brief/voice-style-guide.md` (voice rules), create a content plan for: **$ARGUMENTS** (default to 90 days if no argument).

For each post: title, target keyword, 5-bullet outline, CTA, recommended publish window.

Voice must match the style guide — no generic "10 ways to..." headlines unless that's genuinely the brand. End with a 2-bullet summary of the editorial angle that ties the plan together.
